# Ensure shell scripts always use LF line endings (required for Linux/macOS)
gradlew text eol=lf
*.sh text eol=lf

# Ensure batch files use CRLF (Windows)
*.bat text eol=crlf

# Binary files
*.jar binary
